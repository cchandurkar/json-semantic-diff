/**
 * Built-in demonstration payloads.
 *
 * Framework-free product content: pure JSON plus optional `DiffOptions`. Kept
 * out of `core/` because these are demo material rather than comparison domain
 * logic, and out of the components so large payloads never sit in a template file.
 */
export { DIFF_EXAMPLES } from './diff-examples';
export type { DiffExample } from './diff-examples';
